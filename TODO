# ADMIN
* add bootstrap-os command
* carbon stats
* custom domains (dns auth)
* task retries
* finish app task views
* finish scheduler limits
* cgroups
* task workers should ping admin
* tag (?) log messages so not everything will show up in client ui

# CLIENT
* track task logs
* edit run plans

# METADATA
* adding files
* cron tasks
