# ADMIN
* add bootstrap-os command
* carbon stats
* custom domains (dns auth)
* task retries
* finish app task views
* finish scheduler limits
* cgroups
* task workers should ping admin
* tag (?) log messages so not everything will show up in client ui
* when backend worker starts it can check if upaas/vassal config is changed and try to update apps

# CLIENT
* track task logs
* edit run plans

# METADATA
* cron tasks
* database management
