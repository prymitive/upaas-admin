os:
  Debian: &deb
    packages:
      - git-core
  Ubuntu: *deb

interpreter:
  type: php
  versions:
    - "5.5"

repository:
  clone: git clone --quiet https://github.com/prymitive/dogeweather %destination%
  update:
    - git reset --hard
    - git pull
