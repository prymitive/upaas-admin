language: python

python:
  - 2.6
  - 2.7

install: "pip install -Ur requirements.txt --use-mirrors"

script:
  - py.test -vs --pep8
  - cd upaas_admin && UPAAS_CONFIG_DIR=../tests/ ./manage.py test -v 0 users

services:
  - mongodb
